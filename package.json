{
  "name": "mspm0viaesp32",
  "private": true,
  "version": "0.0.2",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --resolveJsonModule &&node update-version.mjs && vite build",
    "preview": "vite preview",
    "release": "tsc --resolveJsonModule --project tsconfig.build.json"
  },
  "devDependencies": {
    "@types/dom-serial": "^1.0.6",
    "@types/w3c-web-serial": "^1.0.7",
    "typescript": "^5.5.3",
    "vite": "^5.4.1"
  }
}
